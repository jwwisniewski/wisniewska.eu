spring:
  application:
    name: www

server:
  port: 8181

app:
  datasource:
    url: jdbc:h2:file:./data/eu.wisniewska
    username: user
    password: password

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always