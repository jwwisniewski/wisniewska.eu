spring:
  application:
    name: www
  datasource:
    url: jdbc:h2:file:./data/eu.wisniewska
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8181

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

app:
  admin:
    username: admin
    password: qwertyuiop
    role: ADMIN